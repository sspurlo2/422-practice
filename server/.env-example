# COPY THIS FILE AND RENAME IT '.env' WHILE EDITING WITH CORRECT VALUES

# Database Configuration
# Format: postgresql://[username]:[password]@[host]:[port]/[database_name]
# 
# Local PostgreSQL examples:
#   DATABASE_URL=postgresql://postgres:password@localhost:5432/flock_manager
#   DATABASE_URL=postgresql://your_username@localhost:5432/flock_manager
#   DATABASE_URL=postgresql://localhost:5432/flock_manager  (if using peer authentication)
#
# Remote/Cloud PostgreSQL examples:
#   DATABASE_URL=postgresql://user:pass@host.example.com:5432/flock_manager
#   DATABASE_URL=postgresql://user:pass@db.example.com:5432/flock_manager?sslmode=require
#
# To find your local PostgreSQL connection info:
#   1. Check if PostgreSQL is running: psql --version
#   2. Connect to PostgreSQL: psql -U postgres (or your username)
#   3. List databases: \l
#   4. Create database if needed: CREATE DATABASE flock_manager;
#   5. Your connection string will be: postgresql://[username]@localhost:5432/flock_manager
#
# See DATABASE_SETUP.md for detailed instructions
DATABASE_URL=postgresql://postgres:password@localhost:5432/flock_manager

# email info -- to do, follow EMAIL_SETUP.md
# Email Configuration
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
EMAIL_FROM=UOSW Administration <your-email@gmail.com>
